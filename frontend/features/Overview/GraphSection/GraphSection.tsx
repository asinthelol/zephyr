'use client';

import { GraphProps } from '@/shared/components/types';
import { Graph } from '@/shared/components/Graph/Graph';

export function GraphSection({ data }: GraphProps) {
  return (
    <Graph />
  )
}